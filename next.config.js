/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com'],
  },
  // Hier können bei Bedarf weitere Next.js-Konfigurationen hinzugefügt werden
};

module.exports = nextConfig;
